{
  "//": "Shared project hooks. Cross-platform: bare `cargo` invocations (on PATH everywhere), no shell operators or scripts. Runs from the session's project-root cwd. Personal/machine settings live in the gitignored settings.local.json.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "cargo fmt --quiet",
            "timeout": 60
          },
          {
            "type": "command",
            "command": "cargo clippy --workspace --all-targets --quiet -- -D warnings",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
