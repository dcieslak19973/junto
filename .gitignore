# Rust
/target/
**/*.rs.bk

# headroom runtime memory db (not source; lives at repo root)
/headroom_memory.db

# Claude Code personal/machine-local settings (shared config lives in settings.json)
/.claude/settings.local.json

# Serena MCP per-machine project cache (tooling, not source)
/.serena/

# junto per-checkout channel binding (the committed binding is .junto.toml)
.junto.local.toml
